<p id="notice"><%= notice %></p>
<h1>Mini blog</h1>
<table>
  <thead>
 <tr>
   <th>Body</th>
   <th>Like</th>
       <th colspan="3"></th>
     </tr>
   </thead>

   <tbody>
     <% @blogs.each do |blog| %>
       <tr>
         <td><%= link_to blog.body, replyshow_blog_path(blog) %></td>
         <td><%= link_to 'いいね', incriment_blog_path(blog) %><%= blog.like %></td>
         <td><%= link_to 'Show', blog %></td>
         <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
         <td><%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' } %></t    d>
       </tr>
     <% end %>
   </tbody>
</table>

<br>

<%= link_to 'New Blog', new_blog_path %>
